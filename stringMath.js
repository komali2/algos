var stringMath = function(str){
    //we want to break down problem into 'chunks' of positive and negative
    //numbers that can be added to a total

    function recurse(input){
        //the base case would be a negative or positive number

        //otherwise, we need to extract a negative or positive number
    }
}